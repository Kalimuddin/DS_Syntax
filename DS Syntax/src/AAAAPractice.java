import java.util.Arrays;
import java.util.Collections;
import java.util.Scanner;
import java.util.ArrayList;

public class AAAAPractice {
	public static void main(String args[]) {
		/*
		 * Scanner sc = new Scanner(System.in); String s = sc.nextLine(); sc.close();
		 * //System.out.println(s.charAt(0)); String[] aS = s.split(" "); int[] arr =
		 * new int[aS.length]; for(int i=0; i<arr.length; i++) { arr[i] =
		 * Integer.parseInt(aS[i]); } System.out.println(Arrays.toString(arr));
		 */
		
		
		
	}
}
